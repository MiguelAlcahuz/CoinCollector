

<p id="notice"><%= notice %></p>

			
<table class="table table-bordered">
	<thead>
	<tr>
	<th class="formattitle">Collections</th>
	</tr>
	</thead>
	<tbody>
	        
	    		<% @collections.each do |collection|  %>
	    		<tr>
	        <td class="width"><%= collection.currency %></td>
	        <td class="width"> <%= link_to 'My coins', collection_coins_path(collection) %> <%= link_to 'Edit', edit_collection_path(collection) %> <%= link_to 'Destroy', collection, method: :delete, data: { confirm: 'Are you sure?' } %>  </td>
	        </tr>
			
	    <% end %>
	</tbody>
</table>


<%= link_to 'New Collection', new_collection_path %>





